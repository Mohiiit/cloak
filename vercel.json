{
  "buildCommand": "yarn workspace @cloak-wallet/sdk build && yarn workspace @ss-2/nextjs build",
  "outputDirectory": "packages/nextjs/.next",
  "installCommand": "corepack enable && yarn install --immutable",
  "crons": [
    {
      "path": "/api/v1/internal/push/dispatch",
      "schedule": "*/1 * * * *"
    }
  ]
}
