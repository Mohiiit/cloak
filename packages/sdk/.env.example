# Cloak SDK — Environment Variables for Test Scripts
# Copy this file to .env and fill in your values.
#
# NEVER commit the .env file — it is already in .gitignore.

# ─── Starknet RPC ─────────────────────────────────────────────────────
RPC_URL=https://starknet-sepolia.g.alchemy.com/starknet/version/rpc/v0_10/your_alchemy_key_here

# ─── Account 1 (Funder / Declarer) ───────────────────────────────────
# Pre-existing OZ account on Sepolia with STRK balance for funding deploys.
FUNDER_PK=0xyour_funder_private_key_here
FUNDER_ADDRESS=0xyour_funder_address_here

# ─── Account 2 (Guardian) ────────────────────────────────────────────
# Used in ward-related tests (test-ward-setup, test-ward-approval).
GUARDIAN_PK=0xyour_guardian_private_key_here
GUARDIAN_ADDRESS=0xyour_guardian_address_here

# ─── Ward (for test-ward-approval) ───────────────────────────────────
# Address of a deployed CloakWard contract (output of deploy-ward or test-ward-setup).
WARD_ADDRESS=0xyour_ward_address_here

# ─── Supabase ────────────────────────────────────────────────────────
# Used by e2e-full-flow, test-ward-setup, test-ward-approval.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your_supabase_anon_key_here
